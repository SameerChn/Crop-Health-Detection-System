{
  "name": "crop-health-detection-system",
  "version": "1.0.0",
  "description": "Root package for running frontend and backend concurrently",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
